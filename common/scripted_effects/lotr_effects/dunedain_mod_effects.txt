### DUNEDAIN MOD EFFECTS ###
evil_strength_gamerule_buffs = {
	if = {
		limit = { has_game_rule = no_they_arent }
		every_independent_ruler = { 
			limit = { 
				religion = { is_in_family = rf_forcesofevil } 
				NOT = {
					has_character_modifier = evil_weakened_modifier
					has_character_modifier = evil_strengthened_1_modifier
					has_character_modifier = evil_strengthened_2_modifier
					has_character_modifier = evil_strengthened_3_modifier
				}
				NOT = { 
					this = character:lineofsauron 
				}
			} 
			switch = {
				trigger = has_game_rule
				weakened_evil_strength = { add_character_modifier = evil_weakened_modifier }
				strong_evil_strength = { add_character_modifier = evil_strengthened_1_modifier }
				very_strong_evil_strength = { add_character_modifier = evil_strengthened_2_modifier }
				apocalyptic_evil_strength = { add_character_modifier = evil_strengthened_3_modifier }
			}
		}
	}
	else = {
		every_independent_ruler = {  
			limit = { 
				OR = { 
					religion = { is_in_family = rf_forcesofevil } 
					faith = { has_doctrine = doctrine_dominion_of_the_dark }
				}
				NOT = {
					has_character_modifier = evil_weakened_modifier
					has_character_modifier = evil_strengthened_1_modifier
					has_character_modifier = evil_strengthened_2_modifier
					has_character_modifier = evil_strengthened_3_modifier
				}
				NOT = { 
					this = character:lineofsauron 
				}
			} 
			switch = {
				trigger = has_game_rule
				weakened_evil_strength = { add_character_modifier = evil_weakened_modifier }
				strong_evil_strength = { add_character_modifier = evil_strengthened_1_modifier }
				very_strong_evil_strength = { add_character_modifier = evil_strengthened_2_modifier }
				apocalyptic_evil_strength = { add_character_modifier = evil_strengthened_3_modifier }
			}
		}
	}	
}

lotr_upgrade_settlement_holding_one_level = {

	scope:obstacle_province = { 
		if = {
			limit = { has_building = settlement_01 }
			add_building = settlement_02
		}
		else_if = {
			limit = { has_building = settlement_02 }
			add_building = settlement_03
		}
		else = { 
			scope:obstacle_province = { change_development_level = 0.5 }
		}
	}
}

reinstitute_grain_dole_decision_effect_anbalkumagan = {
	every_county_in_region = {
		region = custom_anbalkumagan
		limit = {
			any_county_province = {
				has_building_or_higher = cereal_fields_05
			}
		}
		add_county_modifier = {
			modifier = grain_anbalkumagan_providers_county_modifier
			years = 25
		}
	}
	capital_county = {
		add_county_modifier = {
			modifier = bread_and_circuses_anbalkumagan_county_modifier
			years = 25
		}
	}
	add_legitimacy = medium_legitimacy_gain
	if = {
		limit = {
			NOT = { capital_county = { has_variable = unlocked_grain_dole_anbalkumagan } }
		}
		add_prestige = {
			value = 500
			multiply = 3
		}
		change_influence = {
			value = 500
			multiply = 3
		}
		capital_county = { set_variable = unlocked_grain_dole_anbalkumagan }
	}
	else = {
		add_prestige = 300
		change_influence = 300
	}
}

pharazain_cause_separatism_effect = {
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_bellazen
					}
				}
				
			}
			every_county_in_region  = { 
				region = special_k_bellazen
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_nilulonde
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_nilulonde
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_felaya
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_felaya
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_mardruak
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_mardruak
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_tulwang
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_tulwang
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_an_balkumagan
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_an_balkumagan
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_kharadune
					}
					
				}
			}
			every_county_in_region  = { 
				region = special_k_kharadune
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_gimilthani
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_gimilthani
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_zimrathani
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_zimrathani
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
	ROOT = { 
		if = {
			limit = { 
				NOT = { 
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:separatism_has_been_delt_with_pel
					}
				}
			}
			every_county_in_region  = { 
				region = special_k_pel
				add_county_modifier = lotr_pharazain_separatism_county_modifier
			}
		}
	}
}

convert_to_administrative_from_feudalism_effect_pharazain = { 
	save_scope_as = administrative_liege

	change_to_administrative_effect = yes

	#Direct Vassals who fulfill the vassal_will_become_admin_pharazain criteria are converted. Vassals above 25 opinion always accept
	#Vassals are converted as a hierarchy. If a king is converted we convert all who are valid below that king (so his dukes, their counts, and their barons) are all converted.
	#If a vassal does not convert (because of not accepting or because they are outside of the de jure empire, etc) then the vassals below them are _not_ converted either.

	#Human vassals are given a choice to convert or not in the ping event, even if powerful or having very good relation.

	hidden_effect = { 
		save_scope_as = actor
		every_powerful_vassal = { #All valid powerful vassals auto convert - their support was required to take the decision.
			limit = {
				vassal_will_become_admin_pharazain = yes
				is_ai = yes
			}
			save_scope_as = recipient
			change_to_administrative_effect = yes	
			every_vassal = { #Everyone below also converts
				limit = {
					vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
					is_ai = yes
				}
				save_scope_as = recipient	
				change_to_administrative_effect = yes
				every_vassal = { #Everyone below also converts (could be Counts, could be Barons)
					limit = {
						vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
						is_ai = yes
					}
					save_scope_as = recipient	
					change_to_administrative_effect = yes
					every_vassal = { #Everyone below also converts could be Barons)
						limit = {
							vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
							is_ai = yes
						}
						save_scope_as = recipient
						change_to_administrative_effect = yes
					}
				}
			}
		}
		every_vassal = {
			limit = {
				vassal_will_become_admin_pharazain = yes
				trigger_if = { #Dukes and above can say no
					limit = {
						primary_title.tier >= tier_duchy
						is_ai = yes
					}
					opinion = {
						target = scope:administrative_liege
						value > 25
					}
				}
			}
			save_scope_as = recipient
			change_to_administrative_effect = yes
			every_vassal = { #Everyone below also converts (could be Counts, could be Dukes)
				limit = {
					vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
					is_ai = yes
				}
				save_scope_as = recipient	
				change_to_administrative_effect = yes
				every_vassal = { #Everyone below also converts (Could be Counts, could be Barons)
					limit = {
						vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
						is_ai = yes
					}
					save_scope_as = recipient	
					change_to_administrative_effect = yes
					every_vassal = { #Everyone below also converts (could be Barons)
						limit = {
							vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
							is_ai = yes
						}
						save_scope_as = recipient
						change_to_administrative_effect = yes
					}
				}
			}
		}
		every_vassal = {
			limit = {
				vassal_will_become_admin_pharazain = yes
				trigger_if = { #Dukes and above can say no
					limit = {
						primary_title.tier >= tier_duchy
						is_ai = yes
					}
					opinion = {
						target = scope:administrative_liege
						value > 0
					}
				}
			}
			random_list = {
				10 = { } #10% of the vassals that like us less than 25 but more than 0 still convert.
				90 = {
					save_scope_as = recipient
					change_to_administrative_effect = yes
					every_vassal = { #Everyone below also converts (could be Counts, could be Dukes)
						limit = {
							vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
							is_ai = yes
						}
						save_scope_as = recipient	
						change_to_administrative_effect = yes
						every_vassal = { #Everyone below also converts (Could be Counts, could be Barons)
							limit = {
								vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
								is_ai = yes
							}
							save_scope_as = recipient	
							change_to_administrative_effect = yes
							every_vassal = { #Everyone below also converts (could be Barons)
								limit = {
									vassal_will_become_admin_pharazain = yes #We still don't want mayors and bishops
									is_ai = yes
								}
								save_scope_as = recipient	
								change_to_administrative_effect = yes
							}
						}
					}
				}
			}
		}
		every_vassal = {
			limit = {
				highest_held_title_tier >= tier_duchy
				house.house_head ?= this
				government_has_flag = government_is_administrative
				NOT = {
					any_held_title = { is_noble_family_title = yes }
				}
			}
			create_noble_family_effect = yes
		}
	}

	if = {
		limit = {
			NOT = {
				has_character_flag = latin_emp_force_admin_flag
			}
			is_independent_ruler = yes
		}
		add_character_modifier = {
			modifier = first_admin_emperor
		}
	}
	
	change_influence = admin_convert_influence_value
	if = {
		limit = {
			is_independent_ruler = yes
		}
		add_legitimacy_effect = { LEGITIMACY = admin_convert_legitimacy_value } #One Time Legitimacy Boost
	}

	custom_description_no_bullet = {
		text = vassals_can_become_administrative
	}
	custom_tooltip = powerful_vassals_become_administrative
	custom_tooltip = vassals_become_administrative
	custom_tooltip = vassals_who_may_switch_to_administrative
}