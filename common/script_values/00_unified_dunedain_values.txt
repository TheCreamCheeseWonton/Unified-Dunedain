can_my_culture_seduce_chance = {
    value = 1

    if = {
        limit = { 
			culture = { has_cultural_parameter = seduce_schemes_less_common } 
			NOT = { any_player = { this = prev } }
		}

        multiply = { 0 1 }
    }
}

number_of_adunaic_culture_provinces = {
    value = 0
    every_county_in_region = {
        region = special_nie_extra
        limit = { 
            my_adunaic_culture_trigger = yes
        }
        add = 1
    }
}

number_of_south_bellakar_adunaic_culture_provinces = {
    value = 0
    every_county_in_region = {
        region = special_nie_extra
        limit = { 
            my_south_bellakar_adunaic_culture_trigger = yes
        }
        add = 1
    }
}

number_of_north_adunaic_culture_provinces = {
    value = 0
    every_county_in_region = {
        region = special_nie_extra
        limit = { 
            culture = { has_cultural_pillar = heritage_northern_adunaic }
        }
        add = 1
    }
}

number_of_succeded_contracts_gimilzir_tanturak = {
    value = 0

	if = {
        limit = { 
			has_character_flag = contract_inzilloni_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = contract_azarideth_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = contract_uruloni_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = contract_mispir_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = contract_gimileth_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = contract_arpel_succeded
		}
		add = 1
    }
	if = {
        limit = { 
			has_character_flag = moderate_storm_damage
		}
		add = -1
    }
	if = {
        limit = { 
			has_character_flag = severe_storm_damage
		}
		add = -2
    }
	if = {
        limit = { 
			has_character_flag = catastrophic_storm_damage
		}
		add = -4
    }
} 