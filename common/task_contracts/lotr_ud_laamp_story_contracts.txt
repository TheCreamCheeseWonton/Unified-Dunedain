#####################
## STORY CONTRACTS ##
#####################

### Gimilzir Tanturak Contracts ###

lotr_gimilzir_tanturak_contract_inzilloni = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_inzilloni.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.701
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = inzilloni_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_inzilloni_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_azarideth = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_azarideth.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.711
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = harfalas_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_azarideth_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_uruloni = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_uruloni.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.721
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = uruloni_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_uruloni_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_mispir = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_mispir.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.731
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = mispir_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_mispir_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_gimileth = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_gimileth.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.741
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = gimileth_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_gimileth_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_arpel = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_arpel.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.751
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = arpel_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 100
					add_piety = 150
					custom_tooltip = add_more_settlers_gimilzir
					add_character_flag = contract_arpel_succeded
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}

lotr_gimilzir_tanturak_contract_arpel_sailing = {
	# for grouping together contract types, e.g. mercenary, potentially determines map icon
	group = lotr_story_group
	icon = "gfx/interface/icons/faith/faithful_adunai.dds"
	is_criminal = no
	travel = yes
	use_diplomatic_range = no

	weight = { value = 0 }

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_accept = {
		# Standard triggers.
		scope:employer = { is_landed = yes }
		valid_laamp_basic_accept_only_trigger = yes
		valid_laamp_basic_trigger = {
			EMPLOYER = scope:employer
			LAAMP = root
		}
	}
	valid_to_continue = {
		# Standard triggers.
		valid_laamp_basic_trigger = {
			EMPLOYER = root.task_contract_employer
			LAAMP = root.task_contract_taker
		}
	}
	valid_to_keep = {
	}

	# On_actions
	on_accepted = {
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		
		set_variable = {
			name = task_contract_destination
			value = title:c_arpel.title_province
		}
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		
		task_contract_taker = {
			trigger_event = gimilzir_events.761
		}
	}
	on_create = {
		scope:contract = {
			set_variable = no_success_or_failure
			set_variable = arpel_sailing_contract
		}
	}

	# Rewards root - task_contract
	task_contract_reward = { #
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = {
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract"
					add_prestige = 400
					root.dynasty = { add_dynasty_prestige = 200 }
					add_piety = 250
					add_gold = 200
					if = { 
						limit = {
							this.house ?= { NOT = { has_house_modifier = owns_big_ship } }
						}
						this.house ?= { add_house_modifier = owns_big_ship }
					}
				}
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { 
					play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract"
					add_prestige = -50
					add_piety = -75
				}
			}
		}
	}
}